name: SoulCard Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check workflow
        run: |
          echo "WORKFLOW OK"
          echo "ZIP:"
          ls -lh SoulCardMaster4_arm64_port_v41.zip
